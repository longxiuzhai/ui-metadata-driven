# 开发指南

本文面向第一次接手项目的开发者，覆盖本地环境、源码入口、开发流程、测试和已知问题。

## 1. 环境准备

最低要求：

- JDK 11；
- Maven 3.6+；
- Node.js（建议使用当前维护中的 LTS 版本）；
- npm，依赖安装使用仓库中的 `package-lock.json`。

账号、角色、权限、菜单和客户域演示数据均使用 MySQL 8 持久化，Flyway 在启动时自动建表。Redis不是当前运行必需项。端口约定：MySQL `3306`、后端 `8080`、Vite 开发服务器 `5173`。

检查环境：

```bash
java -version
mvn -version
node --version
npm --version
```

## 2. 首次启动

后端模块相互依赖，先从 `backend` 安装整个 reactor：

```bash
docker compose up -d mysql

cd backend
mvn install
cd application
mvn spring-boot:run
```

前端另开终端：

```bash
cd frontend
npm ci
npm run dev
```

默认管理员为 `admin / Admin123!`。先登录获取 Token，再检查受保护接口：

```bash
curl -X POST 'http://localhost:8080/api/auth/login' \
  -H 'Content-Type: application/json' \
  -d '{"tenantId":"demo","username":"admin","password":"Admin123!"}'

curl 'http://localhost:8080/api/accounts/me' \
  -H 'Authorization: Bearer <上一步返回的 token>'
```

如果前端页面提示元数据加载失败，先直接请求后端接口，再检查 `frontend/vite.config.ts` 中的代理地址和两个进程的端口。

## 3. 从哪里开始读代码

| 目标 | 入口文件 |
|---|---|
| Spring Boot 启动 | `backend/application/src/main/java/com/example/MetadataUiApplication.java` |
| 统一卡片元数据 API | `backend/application/.../page/CardMetadataController.java` |
| 页面装配流程 | `backend/card-platform/.../application/CardPageAssembler.java` |
| Provider 注册与重复检查 | `backend/card-platform/.../registry/CardProviderRegistry.java` |
| 元数据白名单校验 | `backend/card-platform/.../validation/CardDefinitionValidator.java` |
| 完整卡片示例 | `backend/tenant-customization/.../demo/BasicInfoCardProvider.java` |
| 完整编辑动作示例 | `backend/tenant-customization/.../action/CustomerBasicEditActionHandler.java` |
| 前端应用外壳 | `frontend/src/App.vue` |
| 首页/客户详情/客户订单视图 | `frontend/src/views/HomeView.vue`、`CustomerDetailView.vue`、`CustomerOrdersView.vue` |
| 动态卡片页面容器 | `frontend/src/views/MetadataCardPage.vue` |
| 单卡片加载边界 | `frontend/src/components/DynamicCard.vue` |
| 组件/表单/路由白名单 | `frontend/src/components/cards/registry.ts`、`actions/formRegistry.ts`、`actions/routeRegistry.ts` |
| 动作执行入口 | `frontend/src/actions/ActionRuntime.ts` |

推荐先沿客户详情的 `basic_info` 纵向阅读：Provider → 元数据接口 → `CustomerDetailView.vue` → `MetadataCardPage.vue` → `DynamicCard.vue` → `KeyValueCard.vue`。客户订单页面可沿 `CustomerOrdersCardProvider` → `CustomerOrdersView.vue` → `DataTableCard.vue` 阅读。再沿编辑按钮阅读：`CardAction` → `ActionRuntime` → `FormDrawer` → Action API → `CustomerBasicEditActionHandler`。

## 4. 源码与生成文件

前端应修改 `.ts` 和 `.vue` 源文件。`tsconfig.app.json` 已启用 `noEmit`，`npm run build` 只执行类型检查和 Vite 打包，不会在 `src` 下生成对应 JavaScript 文件。

后端 `target/`、前端 `node_modules/` 和 `dist/` 已被 `.gitignore` 忽略。

## 5. 日常开发流程

### 5.1 只改后端卡片定义

1. 在 `tenant-customization` 新增或修改 `CardProvider`；
2. 确认 `pageCode/cardCode`，设置启动期和请求期条件；
3. 复用现有组件时，确保 `props` 与组件消费的数据结构一致；
4. 为统一元数据接口补充 MockMvc 测试；
5. 执行后端测试，并用 `curl` 检查实际响应。

详细模板见[扩展卡片开发指南](扩展卡片开发指南.md)。

### 5.2 新增一种前端卡片组件

这是一项前后端联动修改：

1. 在 `frontend/src/components/cards/` 新增 Vue 组件；
2. 在 `frontend/src/components/cards/registry.ts` 注册稳定的组件编码；
3. 在后端 `CardDefinitionValidator.COMPONENTS` 增加同一编码；
4. 增加组件测试或至少覆盖空数据、错误数据和正常数据；
5. 新增 Provider 示例并验证响应式宽度、加载和失败状态。

仅修改前端注册表会被后端校验拒绝；仅修改后端白名单会在前端落入 `UnknownCard`。

### 5.3 新增业务页面并使用卡片

下面以“订单详情页”为例，从后端到前端新增一个真正可访问的页面。完成后访问：

本节 `curl` 请求受保护接口时均需增加 `Authorization: Bearer <token>`；为突出业务步骤，示例中不重复书写该请求头。

```text
http://localhost:5173/order-detail?orderId=O-1001
```

页面编码先统一，后续所有文件必须使用同一组值：

| 用途 | 示例值 | 出现位置 |
|---|---|---|
| 页面编码 | `order_detail` | Provider、元数据 API、前端 `pageCode` |
| 卡片编码 | `order_basic` | Provider、刷新目标、Action 上下文 |
| 路由名称 | `order-detail` | Vue Router |
| 页面上下文参数 | `orderId` | URL 查询参数、`pageContext`、`dataApi` 占位符 |
| 组件编码 | `KeyValueCard` | Provider；该组件已在前后端白名单注册 |

#### 第 1 步：提供卡片数据接口

在 `backend/tenant-customization/src/main/java/com/example/metadataui/customization/demo/` 新建 `OrderDetailDataController.java`。示例先返回固定数据；真实项目应注入订单 Service，并在接口中校验当前账号是否有权访问该 `orderId`。

```java
package com.example.metadataui.customization.demo;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RestController;

import java.util.Map;

@RestController
public class OrderDetailDataController {
    @GetMapping("/api/orders/{orderId}/basic")
    public Map<String, Object> basic(@PathVariable String orderId) {
        return Map.of(
                "orderNo", orderId,
                "contactName", "张三",
                "status", "CREATED");
    }
}
```

先单独验证数据接口：

```bash
curl 'http://localhost:8080/api/orders/O-1001/basic'
```

#### 第 2 步：新增后端 CardProvider

在同一目录新建 `OrderBasicCardProvider.java`。Provider 只描述卡片，不在 `definition()` 中查询订单数据。

```java
package com.example.metadataui.customization.demo;

import com.example.metadataui.card.condition.CardConditional;
import com.example.metadataui.card.context.CardRequestContext;
import com.example.metadataui.card.model.CardDefinition;
import com.example.metadataui.card.model.ResponsiveSpan;
import com.example.metadataui.card.spi.CardProvider;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.stereotype.Component;

import java.util.List;
import java.util.Map;

@Component
@ConditionalOnProperty(
        name = "metadata.cards.order-basic.enabled",
        havingValue = "true",
        matchIfMissing = true)
@CardConditional(permissions = "order:read")
public class OrderBasicCardProvider implements CardProvider {
    @Override
    public String pageCode() {
        return "order_detail";
    }

    @Override
    public String cardCode() {
        return "order_basic";
    }

    @Override
    public CardDefinition definition(CardRequestContext context) {
        return new CardDefinition(
                cardCode(), "订单信息", "KeyValueCard", 10,
                new ResponsiveSpan(24, 24, 12),
                "/api/orders/{orderId}/basic", "eager", "order:read",
                Map.of("columns", 2, "fields", List.of(
                        Map.of("key", "orderNo", "label", "订单号"),
                        Map.of("key", "contactName", "label", "联系人"),
                        Map.of("key", "status", "label", "状态"))),
                List.of());
    }
}
```

`DemoTenantAutoConfiguration` 已扫描该包，带 `@Component` 的 Provider 和 Controller 会自动注册，不需要再修改 Spring 配置。验证元数据：

```bash
curl 'http://localhost:8080/api/ui/pages/order_detail/cards?orderId=O-1001'
```

响应中应出现 `pageCode=order_detail`、`cards[0].code=order_basic`，且 `dataApi` 仍保留 `{orderId}`，由前端使用页面上下文替换。

#### 第 3 步：补后端集成测试

在 `backend/application/src/test/java/com/example/metadataui/` 新建 `OrderDetailControllerTest.java`。集成测试放在 `application`，因为这里组装了平台和定制模块。

```java
package com.example.metadataui;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;

import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@SpringBootTest
@AutoConfigureMockMvc
class OrderDetailControllerTest {
    @Autowired
    private MockMvc mvc;

    @Test
    void exposesOrderDetailCards() throws Exception {
        mvc.perform(get("/api/ui/pages/order_detail/cards")
                        .param("orderId", "O-1001"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.pageCode").value("order_detail"))
                .andExpect(jsonPath("$.cards[0].code").value("order_basic"));
    }

    @Test
    void hidesCardsWithoutPermission() throws Exception {
        mvc.perform(get("/api/ui/pages/order_detail/cards")
                        .param("orderId", "O-1001")
                        .header("X-Permissions", ""))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.cards").isEmpty());
    }
}
```

#### 第 4 步：增加前端元数据请求

在 `frontend/src/api.ts` 增加：

```ts
export async function getOrderDetailCards(orderId: string): Promise<CardPageDefinition> {
  return getCardPage(
    `/api/ui/pages/order_detail/cards?orderId=${encodeURIComponent(orderId)}`,
    'order_detail'
  )
}
```

不要为每张卡片写数据请求；`DynamicCard` 会读取元数据中的 `dataApi` 并独立加载。

#### 第 5 步：注册页面路由

先新增独立业务 View `frontend/src/views/OrderDetailView.vue`。业务参数、页面上下文和元数据 API 都封装在自己的 View 中，不要修改通用的 `MetadataCardPage.vue`：

```vue
<script setup lang="ts">
import { computed } from 'vue'
import { useRoute } from 'vue-router'
import { getOrderDetailCards } from '../api'
import MetadataCardPage from './MetadataCardPage.vue'

defineProps<{ reloadToken: number }>()

const route = useRoute()
const orderId = computed(() => String(route.query.orderId ?? 'O-1001'))
const pageContext = computed(() => ({ orderId: orderId.value }))

function loadDefinition() {
  return getOrderDetailCards(orderId.value)
}
</script>

<template>
  <MetadataCardPage
    page-code="order_detail"
    :context="pageContext"
    :load-definition="loadDefinition"
    :reload-token="reloadToken"
  />
</template>
```

再在 `frontend/src/router.ts` 导入并注册这个 View：

```ts
import OrderDetailView from './views/OrderDetailView.vue'

{ path: '/order-detail', name: 'order-detail', component: OrderDetailView,
  meta: { title: '订单详情' } }
```

`App.vue` 使用 `RouterView` 渲染路由组件，页面目录读取 `route.meta.title`，因此不需要为新业务页面增加类型、判断分支或模板分支。

如果其他卡片需要通过 `navigate` 动作跳转到订单详情，还要在 `frontend/src/actions/routeRegistry.ts` 增加白名单映射：

```ts
order_detail: params => ({ name: 'order-detail', query: params })
```

后端动作的 `routeCode` 应填写 `order_detail`，而不是直接下发 `/order-detail` URL。

#### 第 6 步：检查页面职责边界

新增业务页面后应满足：

1. `OrderDetailView.vue` 只处理订单详情的路由参数、上下文和元数据请求；
2. `MetadataCardPage.vue` 不出现 `order_detail` 或其他业务页面编码；
3. `App.vue` 不出现订单详情的条件分支；
4. `router.ts` 是业务 View 与 URL 的唯一页面映射入口。

#### 第 7 步：前后端联调

按以下顺序排错：

1. 元数据 API 是否返回 `order_basic`；
2. `dataApi` 是否为站内 `/api/` 地址；
3. `pageContext.orderId` 是否存在，大小写是否与 `{orderId}` 一致；
4. 浏览器 Network 中卡片数据请求是否为 `/api/orders/O-1001/basic`；
5. 无 `order:read` 权限时，元数据是否返回空卡片数组；
6. 执行 `mvn test`、`npm test` 和 `npm run build`。

完成本节后，至少应有这些业务改动：

```text
backend/tenant-customization/.../OrderDetailDataController.java
backend/tenant-customization/.../OrderBasicCardProvider.java
backend/application/.../OrderDetailControllerTest.java
frontend/src/api.ts
frontend/src/router.ts
frontend/src/views/OrderDetailView.vue
frontend/src/actions/routeRegistry.ts   # 仅在需要被 navigate 动作跳转时
```

### 5.4 给卡片新增编辑动作

下面继续给上一节的 `order_basic` 卡片增加“编辑订单”动作。标准链路由项目现有的 `ActionRuntime`、`FormDrawer` 和统一 Action API 负责，新业务只需要提供四部分：卡片动作元数据、后端 Handler、前端表单、注册表映射。

先统一编码：

| 用途 | 示例值 |
|---|---|
| 卡片动作编码 | `edit`，只需在当前卡片内唯一 |
| 表单编码 | `order_basic_edit` |
| 后端动作编码 | `order.basic.update`，全局唯一 |
| 权限编码 | `order:update` |
| 成功后刷新卡片 | `order_basic` |

#### 第 1 步：准备可更新的业务 Repository

Handler 不应把业务数据直接存在自身。为了让示例可以运行，在 `backend/tenant-customization/src/main/java/com/example/metadataui/customization/demo/` 新建 `DemoOrderRepository.java`：

```java
package com.example.metadataui.customization.demo;

import com.example.metadataui.action.exception.ActionConflictException;
import org.springframework.stereotype.Repository;

import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

@Repository
public class DemoOrderRepository {
    private final Map<String, OrderSnapshot> orders = new ConcurrentHashMap<>();

    public OrderSnapshot get(String orderId) {
        return orders.computeIfAbsent(orderId,
                id -> new OrderSnapshot(id, "张三", "CREATED", 1));
    }

    public synchronized OrderSnapshot update(
            String orderId,
            long expectedVersion,
            String contactName,
            String status) {
        OrderSnapshot current = get(orderId);
        if (current.getVersion() != expectedVersion) {
            throw new ActionConflictException("订单已被其他操作修改，请重新加载");
        }
        OrderSnapshot updated = new OrderSnapshot(
                orderId, contactName, status, current.getVersion() + 1);
        orders.put(orderId, updated);
        return updated;
    }

    public static final class OrderSnapshot {
        private final String orderNo;
        private final String contactName;
        private final String status;
        private final long version;

        public OrderSnapshot(
                String orderNo,
                String contactName,
                String status,
                long version) {
            this.orderNo = orderNo;
            this.contactName = contactName;
            this.status = status;
            this.version = version;
        }

        public long getVersion() {
            return version;
        }

        public Map<String, Object> values() {
            return Map.of(
                    "orderNo", orderNo,
                    "contactName", contactName,
                    "status", status);
        }
    }
}
```

然后把上一节的 `OrderDetailDataController` 改为读取该仓库，确保编辑后刷新卡片能看到新值：

```java
private final DemoOrderRepository repository;

public OrderDetailDataController(DemoOrderRepository repository) {
    this.repository = repository;
}

@GetMapping("/api/orders/{orderId}/basic")
public Map<String, Object> basic(@PathVariable String orderId) {
    // 生产环境先校验当前账号对 orderId 的实体数据权限。
    return repository.get(orderId).values();
}
```

这是可运行的内存演示实现。生产项目应由订单领域 Service 操作数据库，并使用数据库乐观锁和事务。

#### 第 2 步：在 Provider 中声明 open-form 动作

修改上一节的 `OrderBasicCardProvider`，补充这些 import：

```java
import com.example.metadataui.card.model.ActionSuccess;
import com.example.metadataui.card.model.ActionTarget;
import com.example.metadataui.card.model.CardAction;
import com.example.metadataui.card.model.ParameterBinding;
```

把 `CardDefinition` 最后一个参数从 `List.of()` 改为：

```java
List.of(new CardAction(
        "edit", "编辑订单", "open-form",
        ActionTarget.form("order_basic_edit", "order.basic.update", "drawer"),
        "order:update",
        Map.of("orderId", ParameterBinding.pageContext("orderId")),
        new ActionSuccess("订单已保存", true, List.of("order_basic"))))
```

这段元数据表达：

- 只有拥有 `order:update` 的用户才能看到按钮；
- 点击后，从 `pageContext.orderId` 解析业务参数；
- 前端只能打开已注册的 `order_basic_edit` 表单；
- 后端只能执行已注册的 `order.basic.update` Handler。

当前实现最终显示的成功消息和刷新列表以 Handler 返回的 `ActionResult` 为准；`ActionSuccess` 保留了协议意图，但不要只配置它而漏掉 Handler 的返回值。

#### 第 3 步：实现后端 UiActionHandler

在 `backend/tenant-customization/src/main/java/com/example/metadataui/customization/demo/action/` 新建 `OrderBasicEditActionHandler.java`：

```java
package com.example.metadataui.customization.demo.action;

import com.example.metadataui.action.model.ActionCommand;
import com.example.metadataui.action.model.ActionContext;
import com.example.metadataui.action.model.ActionPreparation;
import com.example.metadataui.action.model.ActionResult;
import com.example.metadataui.action.spi.UiActionHandler;
import com.example.metadataui.customization.demo.DemoOrderRepository;
import org.springframework.stereotype.Component;

import java.util.List;
import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

@Component
public class OrderBasicEditActionHandler implements UiActionHandler {
    private static final String FORM_CODE = "order_basic_edit";
    private final DemoOrderRepository repository;
    private final Map<String, ActionResult> completedRequests = new ConcurrentHashMap<>();

    public OrderBasicEditActionHandler(DemoOrderRepository repository) {
        this.repository = repository;
    }

    @Override
    public String actionCode() {
        return "order.basic.update";
    }

    @Override
    public String permission() {
        return "order:update";
    }

    @Override
    public boolean supports(ActionContext context) {
        return "order_detail".equals(context.getPageCode())
                && "order_basic".equals(context.getCardCode());
    }

    @Override
    public ActionPreparation prepare(ActionContext context) {
        String orderId = context.requiredParam("orderId");
        DemoOrderRepository.OrderSnapshot order = repository.get(orderId);
        // 在真实项目中，这里还要校验当前租户和用户对该订单的数据权限。
        return new ActionPreparation(FORM_CODE, order.values(), order.getVersion());
    }

    @Override
    public ActionResult execute(ActionContext context, ActionCommand command) {
        String idempotencyKey = context.getTenantId()
                + ":" + context.getUserId()
                + ":" + command.getRequestId();
        ActionResult completed = completedRequests.get(idempotencyKey);
        if (completed != null) {
            return completed;
        }

        String orderId = context.requiredParam("orderId");
        String contactName = requiredString(command.getValues(), "contactName");
        String status = requiredString(command.getValues(), "status");
        if (!List.of("CREATED", "CONFIRMED", "CLOSED").contains(status)) {
            throw new IllegalArgumentException("unsupported order status");
        }

        // update 内必须检查 expectedVersion，并完成实体数据权限和业务校验。
        repository.update(orderId, command.getVersion(), contactName, status);
        ActionResult result = new ActionResult("订单已保存", List.of("order_basic"));
        completedRequests.put(idempotencyKey, result);
        return result;
    }

    private String requiredString(Map<String, Object> values, String name) {
        Object value = values.get(name);
        if (!(value instanceof String) || ((String) value).isBlank()) {
            throw new IllegalArgumentException(name + " is required");
        }
        return (String) value;
    }
}
```

带 `@Component` 的 Handler 会被 `UiActionHandlerRegistry` 自动发现；若 `actionCode()` 重复，应用会在启动时失败。

本项目 bootstrap 已创建 `order:update`。如果是新的业务权限，应先在“权限管理”页面创建权限，再把它分配给目标角色；不要修改 `AccountService` 写死权限。初始权限清单由 `RbacBootstrap` 幂等创建，运行后的业务调整以数据库配置为准。

除了统一权限检查，Handler 仍必须负责：

- 根据 `tenantId/userId/orderId` 做实体数据权限校验；
- 只读取允许修改的字段，不能把 `values` 整体反序列化后直接覆盖实体；
- 校验状态枚举和业务状态流转；
- 使用 `command.version` 做乐观锁；
- 使用 `command.requestId` 做持久化幂等。现有客户示例只使用进程内 Map，不适合多实例生产环境。

#### 第 4 步：新增前端表单组件

在 `frontend/src/forms/OrderBasicEditForm.vue` 新建表单。标准表单组件接收 `modelValue`，每次字段变化都发出新的完整对象：

```vue
<script setup lang="ts">
const props = defineProps<{ modelValue: Record<string, unknown> }>()
const emit = defineEmits<{
  'update:modelValue': [value: Record<string, unknown>]
}>()

function update(key: string, value: string) {
  emit('update:modelValue', { ...props.modelValue, [key]: value })
}
</script>

<template>
  <div class="form-grid">
    <label>
      <span>联系人</span>
      <input
        :value="String(modelValue.contactName ?? '')"
        @input="update('contactName', ($event.target as HTMLInputElement).value)"
      />
    </label>

    <label>
      <span>状态</span>
      <select
        :value="String(modelValue.status ?? 'CREATED')"
        @change="update('status', ($event.target as HTMLSelectElement).value)"
      >
        <option value="CREATED">已创建</option>
        <option value="CONFIRMED">已确认</option>
        <option value="CLOSED">已关闭</option>
      </select>
    </label>
  </div>
</template>
```

输入值只做前端体验校验，不能代替 Handler 的服务端校验。可以复用 `CustomerBasicEditForm.vue` 的样式。

#### 第 5 步：注册表单白名单

修改 `frontend/src/actions/formRegistry.ts`：

```ts
import OrderBasicEditForm from '../forms/OrderBasicEditForm.vue'

export const formRegistry: Record<string, Component> = {
  customer_basic_edit: CustomerBasicEditForm,
  order_basic_edit: OrderBasicEditForm
}
```

`order_basic_edit` 必须与 Provider 的 `ActionTarget.form(...)` 以及 Handler 返回的 `ActionPreparation.formCode` 完全一致。若未注册，`FormDrawer` 会显示“未注册的表单”。

使用现有标准抽屉时，不需要修改以下文件：

- `ActionRuntime.ts`：负责解析 `orderId` 并发出打开表单请求；
- `api.ts`：已有通用 prepare/execute 请求；
- `FormDrawer.vue`：负责加载初始值、提交版本号和 `requestId`、关闭抽屉；
- `App.vue`：收到 `ActionResult.refreshCards` 后更新卡片刷新令牌。

只有新增动作类型或新的展示容器时，才应修改这些平台文件。

#### 第 6 步：补后端动作测试

在 `application` 模块增加集成测试，至少覆盖准备、成功修改、旧版本冲突和无权限直调。结构可参考 `UiActionControllerTest`：

```java
mvc.perform(post("/api/ui/actions/order.basic.update/prepare")
                .contentType(MediaType.APPLICATION_JSON)
                .content("{\"pageCode\":\"order_detail\","
                        + "\"cardCode\":\"order_basic\","
                        + "\"params\":{\"orderId\":\"O-1001\"}}"))
        .andExpect(status().isOk())
        .andExpect(jsonPath("$.formCode").value("order_basic_edit"))
        .andExpect(jsonPath("$.version").isNumber());
```

执行接口请求体必须包含：

```json
{
  "pageCode": "order_detail",
  "cardCode": "order_basic",
  "params": { "orderId": "O-1001" },
  "values": { "contactName": "李四", "status": "CONFIRMED" },
  "version": 1,
  "requestId": "order-update-test-1"
}
```

无权限用例应使用一个只有 `order:read` 的测试账号或 `@WithMockUser(authorities = "order:read")`，直接调用 execute 应返回 `403`；使用旧 version 再提交应返回 `409`；重复使用同一 `requestId` 时应返回第一次结果或明确的幂等冲突。`X-Permissions` 覆盖只在测试配置中开启。

#### 第 7 步：手工联调完整链路

1. 打开订单详情，确认有 `order:update` 权限时显示“编辑订单”；
2. 点击按钮，确认 prepare 返回的初始值进入表单；
3. 保存后确认 execute 请求携带 `orderId/values/version/requestId`；
4. 确认抽屉关闭并提示“订单已保存”；
5. 确认只重新请求 `/api/orders/{orderId}/basic`，没有刷新整个页面；
6. 使用只有 `order:read` 权限的账号请求元数据，确认编辑按钮被移除；
7. 直接无权限调用 execute，确认后端仍返回 `403`。

完成本节后，至少应有这些业务改动：

```text
backend/tenant-customization/.../DemoOrderRepository.java
backend/tenant-customization/.../OrderDetailDataController.java
backend/tenant-customization/.../OrderBasicCardProvider.java
backend/tenant-customization/.../action/OrderBasicEditActionHandler.java
backend/application/.../RbacBootstrap.java              # 新增初始权限时
backend/application/.../OrderActionControllerTest.java
frontend/src/forms/OrderBasicEditForm.vue
frontend/src/actions/formRegistry.ts
```

完整动作协议和安全边界见[页面动作与编辑能力设计方案](页面动作与编辑能力设计方案.md)。

## 6. 条件与权限调试

生产运行时权限来自登录账号的数据库角色。`X-Permissions/X-Features` 覆盖默认关闭，仅在 `src/test/resources/application.yml` 中用于兼容测试，多个值使用逗号分隔，空字符串表示空集合。

```bash
curl 'http://localhost:8080/api/ui/pages/customer_detail/cards?customerId=1001' \
  -H 'X-Permissions: customer:read' \
  -H 'X-Features: customerTags'
```

该测试请求只返回 `basic_info`，并移除需要 `customer:update` 和 `order:read` 的动作。实际联调请使用不同角色账号和 Bearer Token，不要在生产环境开启请求头权限覆盖。

关闭单个 Provider：

```bash
cd backend/application
mvn spring-boot:run \
  -Dspring-boot.run.arguments=--metadata.cards.behavior-trace.enabled=false
```

## 7. 测试与提交前检查

后端：

```bash
# 仓库根目录
mvn test
```

前端：

```bash
cd frontend
npm test
npm run build
```

建议提交前完成：

- 新增 Provider 时覆盖可见、不可见和排序；
- 新增动作时覆盖直接调用无权限和版本冲突；
- 新增参数绑定时覆盖缺失字段和危险路径；
- `npm run build` 无 TypeScript 错误；
- 检查 `git status`，排除意外生成的 `.js`/`.tsbuildinfo`；
- 同步更新 API、注册表和扩展文档。

## 8. 编码约定

- `pageCode`、`cardCode`、`actionCode`、组件编码一旦发布应保持稳定；
- 同一页面内 `cardCode` 唯一，注册器会在启动时拒绝重复项；
- Provider 只描述元数据，不查询卡片主体数据；
- `supports(context)` 只做轻量判断，不执行 SQL 或远程调用；
- `dataApi` 只能使用站内 `/api/` 路径，路径变量来自页面上下文；
- 元数据负责“展示什么”，数据接口和 Action Handler 仍负责真实鉴权；
- 不在元数据中下发 HTML、JavaScript、CSS、SpEL、任意 URL 或外部组件地址。

## 9. 已知问题与技术债

### 前端编译产物进入源码目录

见“源码与生成文件”。这会增加误改和提交噪声，也可能让无扩展名 import 的解析变得难以判断。建议后续单独提交一次清理：设置 TypeScript `noEmit`、删除已跟踪编译产物、补充忽略规则并重新验证构建。

### 演示实现不等于生产实现

账号、角色、权限、菜单以及客户、好友标签、行为轨迹、订单和会员演示数据已经持久化。Feature 和动作幂等记录仍在内存中；数据接口尚未实现实体级鉴权，也没有审计存储、限流和分布式幂等。生产接入检查见[架构说明](架构说明.md#生产化边界)。
