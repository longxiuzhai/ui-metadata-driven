spring:
  datasource:
    url: jdbc:h2:mem:metadata_ui;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true

app:
  security:
    jwt-secret: test-secret-that-is-long-enough-for-hmac-sha256
    token-ttl-seconds: 3600
    permit-all: true
    allow-header-overrides: true
  bootstrap:
    enabled: true
    admin-username: admin
    admin-password: Admin123!
